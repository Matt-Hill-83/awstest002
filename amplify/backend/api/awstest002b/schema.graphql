type Critter @model @auth(rules: [{allow: public}]) {
  id: ID!
}

type Dialog @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String
  text: String
}

type Frame @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String
}

type FrameSet @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String
}
